# Hello World

A small C program used to practice the GitHub workflow in CS-305.

## Build and run

```sh
gcc -Wall -Wextra -std=c11 helloworld.c -o /tmp/helloworld
/tmp/helloworld
```

The `helloworld` branch is used for changes, which are merged into `main`
through pull requests. Both branches are synchronized after each merge.
